<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Интернет нужен и без VPN: загрузка подписки, пинг, проверка обновлений. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Foreground-сервис держит туннель, когда интерфейс закрыт.
         ⚠️ Типа "vpn" в перечне foregroundServiceType НЕ СУЩЕСТВУЕТ — с ним
         манифест не собирается, а на Android 14+ сервис вообще не стартует.
         Правильное значение — systemExempted. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="@string/app_name"
        android:name=".SilentGateApplication"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Ссылки разбираем САМИ (MainActivity -> канал links).
                 С включённым автороутингом Flutter принимает
                 silentgate://import?url=... за имя маршрута, уходит в
                 onUnknownRoute и падает, а импорт не происходит. -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false"/>
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Собственная схема: импорт подписки и управление подключением.
                 Заменяет реестровую регистрацию Windows; разбор ссылок общий
                 (core/url_scheme.dart). launchMode=singleTask + onNewIntent
                 дают то, что на Windows делал single-instance по сокету. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="silentgate"/>
            </intent-filter>

            <!-- Схемы ЧУЖИХ клиентов: люди пересылают друг другу `happ://add/…`
                 и `clash://install-config?url=…`, и при переходе на SilentGate
                 первым делом пробуют именно такую ссылку. Не отзовись мы — и
                 человек решит, что импорт сломан, а не что схема чужая.
                 Разбор — `AppUrlScheme.foreignImportPayload`.
                 ⚠️ Мы не ПОДМЕНЯЕМ эти клиенты: если оригинал установлен,
                 Android покажет выбор приложения, а не откроет нас молча. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="happ"/>
                <data android:scheme="clash"/>
                <data android:scheme="clashmi"/>
                <data android:scheme="stash"/>
                <data android:scheme="sing-box"/>
                <data android:scheme="streisand"/>
                <data android:scheme="v2raytun"/>
                <data android:scheme="hiddify"/>
            </intent-filter>

            <!-- «Поделиться» ссылкой из Telegram или браузера прямо в приложение. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

        <!-- Перехват ссылок серверов вынесен в отдельный alias и ВЫКЛЮЧЕН по
             умолчанию: эти схемы обычно уже привязаны к другому клиенту (Happ,
             v2rayTun), молча забирать их у пользователя нельзя. Тумблер в
             настройках переключает его через PackageManager (задача 74 плана) —
             на Windows ту же роль играет реестр. -->
        <activity-alias
            android:name=".ServerLinksActivity"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="vless"/>
                <data android:scheme="vmess"/>
                <data android:scheme="trojan"/>
                <data android:scheme="ss"/>
                <data android:scheme="hysteria2"/>
                <data android:scheme="hy2"/>
            </intent-filter>
        </activity-alias>

        <!-- Туннель живёт здесь, а не в Activity: процесс интерфейса Android
             убивает независимо от VPN. Это единственное принципиальное
             расхождение с Windows, где движок живёт в процессе окна. -->
        <!-- Плитка быстрых настроек: включить/выключить VPN из шторки.
             directBootAware — чтобы плитка была доступна до разблокировки. -->
        <service
            android:name=".QuickTileService"
            android:exported="true"
            android:icon="@drawable/ic_stat_vpn"
            android:label="@string/app_name"
            android:directBootAware="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true"/>
        </service>

        <service
            android:name=".vpn.SilentGateVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="systemExempted">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Required to query activities that can process text, see:
             https://developer.android.com/training/package-visibility and
             https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

             In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Список установленных приложений для раздельного туннелирования.
             БЕЗ этого объявления getInstalledApplications на targetSdk 30+
             вернёт почти пустой список, и пикер окажется пустым
             (камень §6.6 #45 плана). -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>

        <!-- Проверка «установлен ли Telegram» для кнопки поддержки.
             Объявление выше её НЕ покрывает — нужен отдельный пакет. -->
        <package android:name="org.telegram.messenger"/>
        <package android:name="org.telegram.messenger.web"/>
    </queries>
</manifest>
