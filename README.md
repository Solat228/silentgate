# SilentGate

[![Версия](https://img.shields.io/github/v/release/Solat228/silentgate?label=%D0%B2%D0%B5%D1%80%D1%81%D0%B8%D1%8F&color=7C4DFF)](https://github.com/Solat228/silentgate/releases/latest)
[![Платформы](https://img.shields.io/badge/%D0%BF%D0%BB%D0%B0%D1%82%D1%84%D0%BE%D1%80%D0%BC%D1%8B-Windows%20%7C%20Android-informational)](#технически)
[![Лицензия](https://img.shields.io/badge/%D0%BB%D0%B8%D1%86%D0%B5%D0%BD%D0%B7%D0%B8%D1%8F-GPL--3.0-success)](LICENSE)

SilentGate — VPN-клиент для Windows и Android: VLESS/Reality и Hysteria2 под одним
интерфейсом, без стороннего приложения и его рекламы.

**[⬇ Скачать последнюю версию](https://github.com/Solat228/silentgate/releases/latest)** —
установщик (`SilentGateSetup-*.exe`) или портативная сборка (`SilentGate-Portable-*.zip`)
для Windows, `.apk` для Android.

> ⚠️ Бета. Первый таргет — Windows, следом идёт Android.

## Установка

**Windows** — скачайте `SilentGateSetup-*.exe` и запустите: мастер сам спросит язык и
поставит приложение. Не хочется ставить — берите `SilentGate-Portable-*.zip`,
распакуйте и запускайте `silentgate.exe` из папки.

**Android** — скачайте `.apk` под архитектуру вашего телефона (arm64-v8a — почти все
современные устройства) и установите. Разрешите установку из неизвестных источников,
если система попросит.

Дальше — импортируйте свою ссылку подписки или сервера (кнопка «Импорт» при первом
запуске) и подключайтесь.

## Обновление

С версии 1.14.0 (в работе) приложение обновляется само: находит новую версию в GitHub
Releases, скачивает её, проверяет подпись и контрольную сумму и ставит. По умолчанию оно
сначала спрашивает; в Настройки → Обновления можно выбрать «Автоматически» (живой VPN при
этом не рвётся — установка ждёт отключения) или «Только сообщать». Файл, не прошедший
проверку подписи, не запускается никогда.

- **Windows** — обновляется установленная копия: установщик ставит новую версию на месте и
  перезапускает приложение. Портативная сборка обновляется по-прежнему руками — распакуйте
  новый архив поверх, папку `sg-data` не трогая.
- **Android** — система всегда показывает своё окно установки, тихой установки без root нет.
  При первом обновлении она попросит разрешить установку из SilentGate.

Версии до 1.14.0 так не умеют: на 1.14.0 их нужно обновить вручную, как раньше.

## Поддержка

Что-то не работает — в приложении есть кнопка «Поддержка» (Настройки → О программе):
она собирает диагностический отчёт (версии, настройки, логи, без паролей и адресов
серверов) и подсказывает, куда его отправить.

Баг в самом клиенте или пожелание — заводите
[issue в этом репозитории](https://github.com/Solat228/silentgate/issues).

## Что нового

Версию показывает бейдж выше — он читает последний релиз и не устаревает сам по себе.
Полная история — **[CHANGELOG.md](CHANGELOG.md)**.

### 1.14.0 (в работе) — приложение обновляется само

На Windows и Android приложение скачивает новую версию, проверяет её подпись (ключ зашит в
приложение, сервер обновлений подменить его не может) и контрольную сумму и ставит. Новый
экран «Обновления»: три режима — спрашивать (по умолчанию), автоматически, только сообщать, —
пропуск версии и установка прежних версий. Попутно на Windows кнопка «Скачать» впервые ведёт
на сам установщик, а не на страницу релиза.

### 1.13.2 — подписка снова обновляется через редирект

Приложение запоминало адрес любого редиректа как постоянный — включая временный `302`, которым
новая короткая ссылка на время переезда уводит на старый домен. Из-за этого клиент молча
перепрыгивал на легаси-адрес, менял id профиля и терял автообновление. Теперь адрес запоминается
только при постоянном переезде (301/308), за временным клиент следует лишь ради текущего запроса,
а User-Agent (по нему панель выбирает формат) не теряется ни на одном переходе. ⚠️ Для Android — **бета**.

### 1.13.1 — Android: профили «Авто» пингуются по-настоящему

На телефоне профиль с несколькими узлами считался мёртвым, если не отвечал первый узел —
остальные не спрашивал никто. Теперь спрашиваются все, берётся лучший. Обычные серверы на
Android идут через то же Xray-ядро, что и на ПК, — иначе строгие ноды молча отбрасывали бы
телефоны при обновлении серверов. В «Информации о сервере» появилось время подключения,
карточка подписки на низком окне сворачивается. ⚠️ Для Android этот выпуск — **бета**.

### 1.13.0 — установщик закрывает приложение сам, бета-канал и откат версии

Установщик больше не отправляет человека в трей руками: VPN выключен — закроет молча, VPN
активен — спросит, рвать ли соединение. У обновлений появился отдельный бета-канал по галочке
(по умолчанию выключен) и откат на прежнюю версию. Блок проверок перестал прятать низ главного
экрана: сетка стала 2 колонки на сторону, легенда уехала в подсказку, значки выросли с 18,8 до
28 px. Кнопка подбора сервера переехала к списку серверов и называется глаголом — на экране
больше не три разных «авто».

### 1.12.0 — запас у прямого DNS и запасной путь у выхода

Две подсистемы про то, что раньше молча переставало работать: у резолва трафика
«Прямо» не было запаса (65 % всех ошибок ядра в журнале круглосуточного клиента),
а у выхода раздельного туннелирования — запасного пути. Плюс сторож выходов
больше не выносит приговор вчетверо быстрее положенного.

## Технически

Один Flutter/Dart-интерфейс на все платформы, движок — **Xray-core** (VLESS/XTLS/Reality)
плюс **sing-box** отдельным процессом для TUN-режима и Hysteria2. Оба ядра — отдельные
бинарники, не линкуются в приложение. Подробное обоснование стека, архитектура и
поэтапный план по платформам — в [docs/STACK_DECISION.md](docs/STACK_DECISION.md),
[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) и [docs/ROADMAP.md](docs/ROADMAP.md).

### Сборка из исходников (Windows)

```powershell
# 1. Flutter SDK (см. tools/bootstrap-windows.ps1)
# 2. Ядро xray.exe в engine/windows/bin/
powershell -ExecutionPolicy Bypass -File tools/fetch-xray.ps1
# 3. Запуск
cd app
flutter pub get
flutter run -d windows
```

Подробности разработки Windows-версии — [docs/platforms/WINDOWS.md](docs/platforms/WINDOWS.md).

## Лицензия

Приложение распространяется под **[GNU GPL-3.0](LICENSE)**: форки и производные обязаны
оставаться открытыми под той же лицензией.

Оно поставляется вместе с чужими исполняемыми файлами: **Xray-core** (MPL-2.0, совместима
с GPL-3.0) и **sing-box** (GPL-3.0) идут отдельными процессами, не линкуются; **Wintun** —
своя разрешительная лицензия; гео-базы маршрутизации — CC-BY-SA-4.0. Ядра и гео-файлы в
репозиторий не коммитятся, их скачивают скрипты `tools/fetch-*.ps1` при сборке. Полный
разбор зависимостей — **[THIRD-PARTY.md](THIRD-PARTY.md)**.
